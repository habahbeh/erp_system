# اختبار سريع - 3 دقائق ⏱️

**الهدف**: اكتشاف لماذا التحويلات لا تُحفظ

---

## 🚀 الخطوات السريعة

### 1. شغّل السيرفر
```bash
python manage.py runserver
```

### 2. في terminal آخر، راقب الـ logs
```bash
tail -f django.log
```

### 3. افتح المتصفح
```
http://127.0.0.1:8000/items/44/update/
```

### 4. اذهب للخطوة 3 (التفاصيل والأسعار)

### 5. أضف تحويل:
- من وحدة: كرتون
- المعامل: 12

### 6. احفظ (اضغط التالي → حفظ)

### 7. **راقب django.log فوراً!**

---

## 📊 ماذا تتوقع أن ترى؟

### ✅ إذا كان يعمل:
```
🔄 save_uom_conversions called for item 44
📊 Found 1 conversion fields in POST data
✅ Saved conversion: كرتون → Unit B (factor: 12)
✅ Total conversions saved: 1
```

### ❌ إذا كانت المشكلة:
```
🔄 save_uom_conversions called for item 44
📊 Found 0 conversion fields in POST data  ← المشكلة!
✅ Total conversions saved: 0
```

---

## 📝 أرسل لي النتيجة

**انسخ والصق جميع الرسائل من django.log** التي تظهر بعد "حفظ"

ابحث عن أي رسالة تحتوي على:
- `🔄 save_uom_conversions`
- `📊 Found`
- `✅ Saved` أو `❌ Error`

---

## 🔍 اختبار سريع إضافي

بعد الحفظ، تحقق من قاعدة البيانات:

```bash
python manage.py shell -c "
from apps.core.models import UoMConversion
print('عدد التحويلات:', UoMConversion.objects.filter(item_id=44).count())
"
```

**إذا النتيجة 0** → لم تُحفظ
**إذا النتيجة 1+** → حُفظت لكن لا تُسترجع

---

**نفّذ الاختبار الآن وأرسل لي نتيجة django.log!** 🚀

المدة: 3 دقائق فقط!
