# اختبار حفظ التحويلات + فحص Logs

**الهدف**: اكتشاف لماذا لا تُحفظ التحويلات بعد إضافتها

---

## 🧪 الاختبار الكامل

### الخطوة 1: تشغيل السيرفر

```bash
cd "/Users/mohammadhabahbeh/Desktop/My File/Project/erp_system"
python manage.py runserver
```

---

### الخطوة 2: فتح django.log في terminal آخر

في terminal جديد:
```bash
cd "/Users/mohammadhabahbeh/Desktop/My File/Project/erp_system"
tail -f django.log
```

**اترك هذا Terminal مفتوحاً لمراقبة الـ logs**

---

### الخطوة 3: افتح صفحة التعديل

في المتصفح:
```
http://127.0.0.1:8000/items/44/update/
```

---

### الخطوة 4: انتقل للخطوة 3 (التفاصيل والأسعار)

اضغط "التالي" → "التالي"

---

### الخطوة 5: أضف تحويل جديد

1. اضغط زر **"إضافة تحويل"**
2. اختر "من وحدة": مثلاً **كرتون**
3. أدخل "المعامل": **12**
4. يجب أن تظهر الصيغة: "1 كرتون = 12 Unit B"

---

### الخطوة 6: اذهب للخطوة 4 (المراجعة)

اضغط **"التالي"**

---

### الخطوة 7: احفظ

اضغط زر **"حفظ"**

---

### الخطوة 8: راقب django.log فوراً!

**في Terminal الذي يعرض django.log، يجب أن ترى رسائل مثل:**

#### إذا كان كل شيء يعمل:
```
🔄 save_uom_conversions called for item 44
🗑️ Deleted 1 old conversions
✅ base_uom: Unit B
📊 Found 1 conversion fields in POST data
📋 Conversion fields: ['conversion_from_uom_0']
  Processing conversion index: 0
    from_uom_id: 2, factor: 12
    ✅ Saved conversion: كرتون → Unit B (factor: 12)
✅ Total conversions saved: 1
```

#### إذا كانت هناك مشكلة:
```
🔄 save_uom_conversions called for item 44
🗑️ Deleted 1 old conversions
✅ base_uom: Unit B
📊 Found 0 conversion fields in POST data  ❌ المشكلة هنا!
📋 Conversion fields: []
✅ Total conversions saved: 0
```

---

## 📊 تحليل النتائج

### النتيجة 1: "Found 0 conversion fields in POST data"

**المشكلة**: البيانات لا تُرسل في POST request

**السبب المحتمل**:
- الحقول ليست داخل `<form>` tag
- JavaScript لا يُنشئ الحقول بشكل صحيح
- الحقول مُخفية أو disabled

**الحل**: سأصلح template

---

### النتيجة 2: "Found 1 conversion fields" لكن "Skipping - missing data"

**المشكلة**: الحقل موجود لكن القيمة فارغة

**السبب المحتمل**:
- اسم الحقل خاطئ
- القيمة لا تُملأ

**الحل**: سأصلح أسماء الحقول

---

### النتيجة 3: "Found 1 conversion fields" و "Saved conversion"

**المشكلة**: البيانات تُحفظ لكن لا تظهر عند إعادة الفتح!

**السبب المحتمل**:
- مشكلة في استرجاع البيانات
- مشكلة في context

**الحل**: سأصلح دالة الاسترجاع

---

## 📝 أرسل لي النتيجة

بعد تنفيذ الخطوات أعلاه، **انسخ والصق جميع الرسائل من django.log** التي تظهر بعد الضغط على "حفظ".

ابحث عن الرسائل التي تحتوي على:
- 🔄 save_uom_conversions
- 📊 Found
- ✅ Saved
- ❌ Error

---

## 🔍 اختبار إضافي: تحقق من قاعدة البيانات

بعد الحفظ، تحقق من قاعدة البيانات مباشرة:

```bash
python manage.py shell -c "
from apps.core.models import Item, UoMConversion

item = Item.objects.get(pk=44)
conversions = UoMConversion.objects.filter(item=item)

print(f'عدد التحويلات في قاعدة البيانات: {conversions.count()}')

for conv in conversions:
    print(f'  - {conv.from_uom.name}: {conv.conversion_factor}')
"
```

**إذا أعطى**: `عدد التحويلات: 0` → البيانات لم تُحفظ

**إذا أعطى**: `عدد التحويلات: 1` → البيانات حُفظت! المشكلة في الاسترجاع

---

## 🎯 الخطوات التالية

بناءً على نتيجة django.log، سأعرف بالضبط ما المشكلة وسأحلها فوراً.

**أرسل لي**:
1. جميع رسائل django.log بعد الضغط على "حفظ"
2. نتيجة الاستعلام على قاعدة البيانات

---

**جاهز للاختبار؟** ابدأ الآن! ⏱️
