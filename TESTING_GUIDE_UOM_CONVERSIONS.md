# دليل اختبار تحويلات وحدات القياس

**التاريخ**: 2025-11-19
**الحالة**: ✅ جاهز للاختبار

---

## 🧪 سيناريوهات الاختبار

### الحالة الحالية للبيانات:

```
📦 المادة #42: مسمار
   الوحدة الأساسية: test333
   عدد التحويلات: 1
   - جرام: 0.001000
     الصيغة: 1 جرام = 0.001 test333
```

---

## اختبار 1: عرض التحويلات الموجودة (Retrieve)

### الخطوات:

1. **افتح المتصفح**
   ```
   http://127.0.0.1:8000/items/42/update/
   ```

2. **انتقل إلى الخطوة 3** (التفاصيل والأسعار)

3. **تحقق من جدول التحويلات**

### النتيجة المتوقعة:

```
┌────────────────────────────────────────────────────────┐
│ تحويلات وحدات القياس                                  │
│ التحويل يتم إلى وحدة القياس الأساسية للمادة          │
├──────────────┬──────────┬───────────────────┬─────────┤
│ من وحدة      │ المعامل  │ الصيغة            │ الإجراء │
├──────────────┼──────────┼───────────────────┼─────────┤
│ جرام  ▼      │ 0.001    │ 1 جرام = 0.001    │   [×]   │
│              │          │ test333           │         │
└──────────────┴──────────┴───────────────────┴─────────┘
             [+ إضافة تحويل]
```

**التحقق**:
- ✅ الصف يظهر مع التحويل الموجود
- ✅ dropdown "من وحدة" يعرض "جرام" محدد
- ✅ حقل "المعامل" يعرض "0.001"
- ✅ عمود "الصيغة" يعرض "1 جرام = 0.001 test333"

---

## اختبار 2: إضافة تحويل جديد (Create)

### الخطوات:

1. **ابدأ من نفس الصفحة** (http://127.0.0.1:8000/items/42/update/)

2. **اضغط زر "إضافة تحويل"**

3. **املأ البيانات**:
   - من وحدة: اختر "كيلو" (أو أي وحدة أخرى)
   - المعامل: 1

4. **لاحظ الصيغة** يجب أن تظهر تلقائياً:
   ```
   1 كيلو = 1 test333
   ```

5. **احفظ المادة** (اضغط زر الحفظ في نهاية الصفحة)

6. **أعد فتح الصفحة**:
   ```
   http://127.0.0.1:8000/items/42/update/
   ```

7. **انتقل للخطوة 3** وتحقق من الجدول

### النتيجة المتوقعة:

```
┌────────────────────────────────────────────────────────┐
│ تحويلات وحدات القياس                                  │
├──────────────┬──────────┬───────────────────┬─────────┤
│ من وحدة      │ المعامل  │ الصيغة            │ الإجراء │
├──────────────┼──────────┼───────────────────┼─────────┤
│ جرام  ▼      │ 0.001    │ 1 جرام = 0.001    │   [×]   │
│              │          │ test333           │         │
├──────────────┼──────────┼───────────────────┼─────────┤
│ كيلو  ▼      │ 1        │ 1 كيلو = 1        │   [×]   │
│              │          │ test333           │         │
└──────────────┴──────────┴───────────────────┴─────────┘
```

**التحقق**:
- ✅ التحويل الجديد تم حفظه
- ✅ التحويل القديم ما زال موجود
- ✅ كلا التحويلين يعرضان الصيغة بشكل صحيح

---

## اختبار 3: تعديل تحويل موجود

### الخطوات:

1. **افتح** http://127.0.0.1:8000/items/42/update/

2. **انتقل للخطوة 3**

3. **عدّل المعامل** للتحويل الأول:
   - غيّر من 0.001 إلى 0.01

4. **لاحظ الصيغة** يجب أن تتحدث تلقائياً:
   ```
   1 جرام = 0.01 test333
   ```

5. **احفظ**

6. **أعد فتح** الصفحة وتحقق

### النتيجة المتوقعة:

- ✅ المعامل الجديد (0.01) تم حفظه
- ✅ الصيغة تعرض القيمة الجديدة

---

## اختبار 4: حذف تحويل

### الخطوات:

1. **افتح** http://127.0.0.1:8000/items/42/update/

2. **انتقل للخطوة 3**

3. **اضغط زر [×]** بجانب أحد التحويلات

4. **لاحظ**: الصف يختفي من الجدول

5. **احفظ**

6. **أعد فتح** الصفحة

### النتيجة المتوقعة:

- ✅ التحويل المحذوف لم يعد موجوداً
- ✅ التحويلات الأخرى ما زالت موجودة

---

## اختبار 5: إضافة مادة جديدة مع تحويلات

### الخطوات:

1. **افتح** http://127.0.0.1:8000/items/create/

2. **الخطوة 1**: املأ البيانات الأساسية
   - الاسم: مادة اختبار تحويلات
   - الكود: TEST-CONV-001
   - اختر وحدة قياس أساسية: قطعة (piece)

3. **الخطوة 2**: تجاوز (لا تضف متغيرات)

4. **الخطوة 3**: أضف تحويلات
   - إضافة تحويل 1:
     - من وحدة: كرتون
     - المعامل: 12
     - الصيغة: 1 كرتون = 12 قطعة

   - إضافة تحويل 2:
     - من وحدة: دزينة
     - المعامل: 12
     - الصيغة: 1 دزينة = 12 قطعة

5. **احفظ**

6. **افتح المادة للتعديل**

7. **تحقق** من أن التحويلات محفوظة وتظهر

### النتيجة المتوقعة:

- ✅ التحويلات تُحفظ بشكل صحيح
- ✅ عند التعديل، التحويلات تظهر
- ✅ الصيغ تعرض بشكل صحيح

---

## اختبار 6: التحقق من قاعدة البيانات

### الأمر:

```bash
cd "/Users/mohammadhabahbeh/Desktop/My File/Project/erp_system"
python manage.py shell -c "
from apps.core.models import Item, UoMConversion

# استبدل 42 برقم المادة التي أنشأتها
item_id = 42
item = Item.objects.get(pk=item_id)

print(f'المادة: {item.name}')
print(f'الوحدة الأساسية: {item.base_uom.name if item.base_uom else \"غير محددة\"}')
print(f'عدد التحويلات: {item.uom_conversions.count()}')
print()

for conv in item.uom_conversions.all():
    print(f'- من: {conv.from_uom.name}')
    print(f'  المعامل: {conv.conversion_factor}')
    print(f'  الصيغة: {conv.formula_expression}')
    print(f'  الملاحظات: {conv.notes}')
    print()
"
```

### النتيجة المتوقعة:

```
المادة: مسمار
الوحدة الأساسية: test333
عدد التحويلات: 2

- من: جرام
  المعامل: 0.001000
  الصيغة: 1 جرام = 0.001 test333
  الملاحظات: تحويل من جرام إلى الوحدة الأساسية test333

- من: كيلو
  المعامل: 1.000000
  الصيغة: 1 كيلو = 1 test333
  الملاحظات: تحويل من كيلو إلى الوحدة الأساسية test333
```

---

## ❌ الأخطاء المحتملة

### خطأ 1: التحويلات لا تظهر

**الأعراض**:
- الجدول فارغ عند فتح صفحة التعديل

**التشخيص**:
1. افتح Console (F12)
2. ابحث عن أخطاء JavaScript
3. تحقق من أن `existingConversions` موجود في source code

**الحل**:
- تحقق من أن `existing_conversions_json` يُمرر في context

---

### خطأ 2: الصيغة لا تتحدث

**الأعراض**:
- عند تغيير "من وحدة" أو "المعامل"، الصيغة لا تتحدث

**التشخيص**:
- افتح Console وابحث عن أخطاء في `updateConversionFormula()`

**الحل**:
- تحقق من أن `onchange` handlers مضافة
- تحقق من أن `base_uom` محدد

---

### خطأ 3: التحويلات لا تُحفظ

**الأعراض**:
- عند الحفظ، لا يظهر خطأ
- لكن عند إعادة فتح الصفحة، التحويلات اختفت

**التشخيص**:
```bash
# تحقق من logs
tail -f django.log

# أو تحقق من database
python manage.py shell -c "
from apps.core.models import UoMConversion
print(UoMConversion.objects.count())
"
```

**الحل**:
- تحقق من أن `save_uom_conversions()` تُستدعى
- تحقق من أن `base_uom` محدد للمادة

---

## ✅ معايير النجاح

اعتبر الاختبار **ناجح** إذا:

- ✅ يمكن إضافة تحويلات جديدة
- ✅ التحويلات تُحفظ في قاعدة البيانات
- ✅ التحويلات تظهر عند إعادة فتح صفحة التعديل
- ✅ يمكن تعديل تحويل موجود
- ✅ يمكن حذف تحويل
- ✅ الصيغة تعرض بشكل صحيح
- ✅ الصيغة تتحدث ديناميكياً عند التغيير
- ✅ لا توجد أخطاء في Console
- ✅ لا توجد أخطاء في Django logs

---

## 📝 تقرير الاختبار

بعد إجراء جميع الاختبارات، املأ هذا النموذج:

```
تاريخ الاختبار: _______________
المختبر: _______________

┌─────────────────────────────────────────┬─────┬─────┐
│ الاختبار                                │ نجح │ فشل │
├─────────────────────────────────────────┼─────┼─────┤
│ عرض التحويلات الموجودة                 │ [ ] │ [ ] │
│ إضافة تحويل جديد                        │ [ ] │ [ ] │
│ تعديل تحويل موجود                       │ [ ] │ [ ] │
│ حذف تحويل                               │ [ ] │ [ ] │
│ إضافة مادة جديدة مع تحويلات            │ [ ] │ [ ] │
│ التحقق من قاعدة البيانات                │ [ ] │ [ ] │
└─────────────────────────────────────────┴─────┴─────┘

الملاحظات:
_____________________________________________________
_____________________________________________________
_____________________________________________________

الحالة النهائية: [ ] جاهز للإنتاج  [ ] يحتاج إصلاحات
```

---

**تم إنشاؤه بواسطة**: Claude Code
**التاريخ**: 2025-11-19
**الحالة**: ✅ جاهز للاختبار الفوري
