# دليل توحيد الخطوط والأرقام العربية

## التغييرات المطبقة

### 1. توحيد الخطوط

تم توحيد الخطوط المستخدمة في جميع صفحات النظام:

**في `templates/base/base.html`:**
- أضيف خط Roboto بجانب Cairo
```html
<link href="https://fonts.googleapis.com/css2?family=Cairo:wght@200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
```

**في `static/css/main.css`:**
- تم تحديث font-family
```css
body {
    font-family: 'Cairo', 'Roboto', sans-serif;
}
```

---

### 2. الأرقام العربية الهندية

تم إضافة نظام تلقائي لتحويل الأرقام إلى العربية الهندية (٠١٢٣٤٥٦٧٨٩)

#### الطريقة الأولى: JavaScript (تلقائي)

**في `static/js/main.js`:**
- أضيفت دالة `toArabicNumbers()` لتحويل الأرقام
- أضيفت دالة `convertNumbersInElement()` لتحويل جميع الأرقام في عنصر HTML
- أضيفت دالة `initArabicNumbers()` التي تعمل تلقائياً عند تحميل الصفحة
- استخدام MutationObserver لمراقبة المحتوى الديناميكي

**التفعيل:**
الكود يعمل تلقائياً عند تحميل أي صفحة! لا حاجة لأي إعدادات إضافية.

#### الطريقة الثانية: Django Template Tags (اختياري)

**في `apps/core/templatetags/arabic_numbers.py`:**

تم إنشاء 3 فلاتر يمكن استخدامها في القوالب:

##### 1. `arabic_numbers` - تحويل بسيط
```django
{% load arabic_numbers %}

{{ 12345|arabic_numbers }}
{# النتيجة: ١٢٣٤٥ #}
```

##### 2. `format_number_ar` - تنسيق مع فواصل
```django
{% load arabic_numbers %}

{{ 1234567.89|format_number_ar:2 }}
{# النتيجة: ١،٢٣٤،٥٦٧٫٨٩ #}

{{ 5000|format_number_ar }}
{# النتيجة: ٥،٠٠٠ #}
```

##### 3. `currency_ar` - تنسيق كعملة
```django
{% load arabic_numbers %}

{{ 1234.567|currency_ar }}
{# النتيجة: ١،٢٣٤٫٥٦٧ دينار #}

{{ 1234.5|currency_ar:'USD' }}
{# النتيجة: ١،٢٣٤٫٥٠ دولار #}

{{ 1234.5|currency_ar:'EUR' }}
{# النتيجة: ١،٢٣٤٫٥٠ يورو #}
```

---

## استخدام في القوالب

### مثال كامل في قالب Django:

```django
{% extends 'base/base.html' %}
{% load i18n arabic_numbers %}

{% block content %}
<div class="container">
    <h2>الإحصائيات</h2>

    <!-- الأرقام ستتحول تلقائياً بواسطة JavaScript -->
    <div class="stats">
        <div>عدد الأصول: {{ total_assets }}</div>
        <div>القيمة: {{ total_value }}</div>
    </div>

    <!-- أو استخدام الفلاتر للتحكم الدقيق -->
    <div class="formatted-stats">
        <div>عدد الأصول: {{ total_assets|arabic_numbers }}</div>
        <div>القيمة: {{ total_value|currency_ar }}</div>
        <div>المعدل: {{ average|format_number_ar:2 }}</div>
    </div>
</div>
{% endblock %}
```

---

## الفرق بين الطريقتين

| الميزة | JavaScript | Template Tags |
|--------|-----------|---------------|
| تلقائي | ✅ نعم | ❌ لا |
| يحتاج كود في القالب | ❌ لا | ✅ نعم |
| يعمل مع AJAX | ✅ نعم | ❌ لا |
| السرعة | بطيء قليلاً | سريع |
| التحكم الدقيق | ❌ محدود | ✅ كامل |

---

## توصيات الاستخدام

1. **للصفحات العادية:** اترك JavaScript يعمل تلقائياً
2. **للبيانات المهمة (الفواتير، التقارير):** استخدم Template Tags للتحكم الدقيق
3. **للجداول الكبيرة:** استخدم Template Tags لتحسين الأداء

---

## أمثلة في الواقع

### صفحة لوحة التحكم:
```django
{% load arabic_numbers %}

<div class="stat-card">
    <h3>إجمالي الإيرادات</h3>
    <div class="stat-value">{{ total_revenue|currency_ar }}</div>
</div>
```

### جدول الأصول:
```django
{% load arabic_numbers %}

<table>
    {% for asset in assets %}
    <tr>
        <td>{{ asset.asset_number|arabic_numbers }}</td>
        <td>{{ asset.name }}</td>
        <td>{{ asset.purchase_price|currency_ar }}</td>
        <td>{{ asset.book_value|format_number_ar:3 }}</td>
    </tr>
    {% endfor %}
</table>
```

---

## تعطيل تحويل الأرقام التلقائي

إذا أردت تعطيل التحويل التلقائي لصفحة معينة، أضف class خاص:

```html
<div class="no-arabic-numbers">
    هذا الرقم 12345 لن يتحول
</div>
```

ثم أضف في `main.js`:
```javascript
// في دالة initArabicNumbers()
// تخطي العناصر ذات class معين
if (node.parentElement && node.parentElement.classList.contains('no-arabic-numbers')) {
    return;
}
```

---

## الخلاصة

✅ **تم توحيد الخطوط**: Cairo + Roboto في جميع الصفحات
✅ **تحويل الأرقام تلقائياً**: عبر JavaScript
✅ **Template Tags إضافية**: للتحكم الدقيق عند الحاجة

الآن جميع صفحات النظام (`/`, `/assets/`, `/accounting/`) تستخدم نفس الخط ونفس عرض الأرقام! 🎉
