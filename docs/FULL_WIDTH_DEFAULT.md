# تحديث: عرض كامل افتراضياً
## Update: Full Width by Default

تاريخ: 2025-11-09

---

## ✅ ما تم تغييره؟

تم تحديث صفحة فاتورة المشتريات لتكون **عرض كامل افتراضياً**.

### قبل التحديث:
```
عند فتح الصفحة:
┌──────────────────┬────────────┐
│                  │            │
│   الجدول (66%)   │ الشريط 33% │
│                  │            │
└──────────────────┴────────────┘

المستخدم يحتاج لـ:
1. الضغط على الزر
2. أو الضغط على F8
   → لتوسيع الجدول
```

### بعد التحديث:
```
عند فتح الصفحة:
┌──────────────────────────────┐
│                              │
│   الجدول (100% - عرض كامل)   │
│                              │
└──────────────────────────────┘

الشريط الجانبي مخفي تلقائياً!
زر "إظهار" موجود للوصول للشريط عند الحاجة
```

---

## 🎯 الفوائد

### 1. مساحة عمل أكبر فوراً
- لا حاجة لإخفاء الشريط يدوياً
- تبدأ العمل مباشرة بمساحة كاملة
- أفضل للإدخال السريع

### 2. أنسب للشاشات الصغيرة
- مثالي للابتوب 13-15 بوصة
- استغلال كامل للمساحة المتاحة
- أقل تمرير أفقي

### 3. سير عمل محسّن
```
قبل:
فتح الصفحة → F8 لإخفاء الشريط → ابدأ الإدخال

بعد:
فتح الصفحة → ابدأ الإدخال فوراً! ✅
```

---

## 🔧 كيف يعمل الآن؟

### عند فتح الصفحة:
1. الشريط الجانبي **مخفي**
2. الجدول **100% عرض**
3. الزر يقول **"إظهار"**

### عند الحاجة للشريط (الإجماليات، التفقيط...):
1. اضغط **F8** أو انقر الزر
2. الشريط يظهر من اليسار
3. الجدول يتقلص لـ 66%

### عند العودة للإدخال:
1. اضغط **F8** مرة أخرى
2. الشريط يختفي
3. الجدول يتوسع لـ 100%

---

## 💾 حفظ التفضيلات

النظام يحفظ اختيارك:

### إذا أظهرت الشريط:
```
1. تضغط F8 → الشريط يظهر
2. localStorage يحفظ: 'visible'
3. المرة القادمة: الشريط سيظهر تلقائياً
```

### إذا أخفيت الشريط (أو لم تغير شيء):
```
1. تترك الشريط مخفي (الافتراضي)
2. localStorage يحفظ: 'hidden'
3. المرة القادمة: الشريط سيبقى مخفي (افتراضي)
```

---

## 🎨 التفاصيل التقنية

### ما تم تعديله:

#### 1. CSS الافتراضي
```css
/* قبل: الشريط مرئي افتراضياً */
.sidebar-area {
    max-width: 33.333333%;
    flex: 0 0 33.333333%;
}

/* بعد: الشريط مخفي افتراضياً */
.sidebar-area {
    max-width: 0 !important;
    flex: 0 0 0 !important;
    opacity: 0;
}
```

#### 2. JavaScript
```javascript
// قبل
if (savedState === 'hidden') {
    toggleSidebar(false);
}
// افتراضياً: مرئي

// بعد
if (savedState === 'visible') {
    toggleSidebar(true);
} else {
    toggleSidebar(false); // افتراضياً: مخفي
}
```

#### 3. HTML الزر
```html
<!-- قبل -->
<button>
    <i class="fas fa-angle-double-left"></i>
    <span>إخفاء</span>
</button>

<!-- بعد -->
<button>
    <i class="fas fa-angle-double-right"></i>
    <span>إظهار</span>
</button>
```

---

## 📊 سيناريوهات الاستخدام

### السيناريو 1: مستخدم جديد (أول مرة)
```
1. يفتح الصفحة
   → الشريط مخفي (100% عرض)
2. يدخل البيانات بسهولة
3. يحتاج رؤية الإجماليات
   → يضغط F8
4. يراجع الإجماليات
5. يضغط F9 للحفظ
```

### السيناريو 2: مستخدم يفضل الشريط دائماً
```
1. يفتح الصفحة
   → الشريط مخفي (افتراضي)
2. يضغط F8 فوراً
   → الشريط يظهر
3. النظام يحفظ الاختيار
4. المرة القادمة:
   → الشريط سيظهر تلقائياً!
```

### السيناريو 3: مستخدم يفضل العرض الكامل
```
1. يفتح الصفحة
   → الشريط مخفي ✅ (مثالي!)
2. يعمل مباشرة
3. لا يغير شيء
4. كل مرة يفتح الصفحة:
   → عرض كامل دائماً
```

---

## 🔄 التراجع عن التحديث

إذا أردت العودة للسلوك القديم (الشريط مرئي افتراضياً):

### الطريقة 1: Git
```bash
git checkout apps/purchases/templates/purchases/invoices/invoice_form.html
```

### الطريقة 2: تعديل يدوي
في ملف `invoice_form.html`، ابحث عن:

```javascript
if (savedState === 'visible') {
    toggleSidebar(true);
} else {
    toggleSidebar(false); // ← احذف هذا السطر
}
```

غيّره إلى:

```javascript
if (savedState === 'hidden') {
    toggleSidebar(false);
}
// افتراضياً: مرئي
```

---

## ✅ الخلاصة

### التغييرات:
1. ✅ الشريط الجانبي **مخفي افتراضياً**
2. ✅ الجدول **100% عرض** عند الفتح
3. ✅ الزر يقول **"إظهار"** بدلاً من "إخفاء"
4. ✅ حفظ التفضيلات ما زال يعمل

### الفوائد:
- 🚀 **بداية أسرع** - مساحة كاملة فوراً
- 📏 **مساحة أكبر** - 100% بدلاً من 66%
- ⌨️ **أقل خطوات** - لا حاجة لـ F8 عند البداية
- 💾 **ذكي** - يتذكر اختيارك

### كيفية الاستخدام:
```
الافتراضي: عرض كامل (الشريط مخفي)
F8: إظهار/إخفاء الشريط
localStorage: يحفظ اختيارك
```

---

## 📞 إذا واجهت مشكلة

### المشكلة: الشريط يظهر عند الفتح
**السبب**: localStorage محفوظ كـ 'visible' من قبل
**الحل**:
```javascript
// في Console المتصفح (F12)
localStorage.removeItem('invoice_sidebar_state');
location.reload();
```

### المشكلة: أريد الشريط مرئياً دائماً
**الحل**:
1. افتح الصفحة
2. اضغط F8 → الشريط يظهر
3. النظام يحفظ اختيارك
4. كل مرة تفتح الصفحة سيظهر تلقائياً

---

تم التحديث في: 2025-11-09
الإصدار: 2.0 (Full Width Default)
